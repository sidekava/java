자바 객체 24강 추상 메소드

원, 사격형, 선의 공통분모로 도형 클래스를 만든다. 도형 클래스는 추상 클래스로 만든다.

그 장점으로 도형을 배열로 만들면 원 사각형 선을 담을 수 있다. 그리고 도형에 공통기능을 구현함으로써 코드 집중화가 가능하다.

도형을 구현하는 클래스에는 모두 a라는 메소드가 있으나 그 내용은 다르다. 그래서 추상클래스에 구현할땐 이름만 빌려오는 식, 즉 뼈대만 구현해야 한다. 그 메소드를 추상 메소드라 한다.